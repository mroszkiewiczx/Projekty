name: Deploy to Hostinger

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Manual trigger (test)
        run: |
          echo "✅ GitHub Actions would trigger webhook here"
          echo "For now: Push to main = Deploy via GitHub webhook (when ready)"
          echo "Deploy config: .github/workflows/deploy.yml"
